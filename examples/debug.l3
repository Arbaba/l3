;(letrec ((f1 (fun (x) 
;            (if (@= x 1) x
;                 (f1 (@+ 1 x)))
;            )
;        ))
;       (f1 0)
;)
;(if #t #t #f)

(letrec (( f1 (fun (x)
            (let*  ((a  1)
                    (b  2)
                    )
                    (if (@= b x) x (f1(@+ x 1)))
            ))))
            (f1 0)
)